// Function with parameter and return type
function greet(name: string): string {
    return `Hello, ${name}`;
}

// Call the function
let myname: string = greet("Daya");
console.log(myname);  // Output: Hello, Daya
